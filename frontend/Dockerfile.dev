# Development用Dockerfile (ホットリロード対応)
FROM node:20-alpine

WORKDIR /app

# 依存関係のインストール
COPY package.json package-lock.json* ./
RUN npm install

# ソースコードをコピー
COPY . .

EXPOSE 3000

CMD ["npm", "run", "dev"]
